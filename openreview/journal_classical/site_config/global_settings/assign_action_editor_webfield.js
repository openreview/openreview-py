/*
 * Paper-scoped Assign Action Editor webfield.
 *
 * The generated OpenReview webfield is assembled by scripts/build/site_config.py
 * from assign_action_editor_webfield_parts/*.js. This source is intentionally
 * copied from the EIC assignment UI so the paper-forum invitation owns the
 * paper-specific assignment workflow independently of the EIC console.
 */

if (typeof module === 'object' && module.exports) {
  module.exports = require('./assign_action_editor_webfield_parts/99_render.js')
}
